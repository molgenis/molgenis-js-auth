title: FusionAuth Role Manager
email: s.haakma@rug.nl
description: >- # this means to ignore newlines until "baseurl:"
  This is the FusionAuth Role Manager which can be used to manage roles and users for applications configured in FusionAuth.
url: "https://molgenis.github.io"
base_url: "/molgenis-js-auth"
github_username: sidohaakma

heading_anchors: true
permalink: pretty
remote_theme: pmarsceill/just-the-docs 

include: [_pages]

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
  - Gemfile
  - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
